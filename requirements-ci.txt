# Lean dependency set for CI: import + lint + type-check + pytest.
# Excludes tensorflow / torch / keras / jupyter / plotting extras that the
# test suite and static analysis do not touch. Install the full stack from
# requirements.txt for training and the dashboard.

# Core
pandas>=2.2.0
numpy>=1.26.3
scipy>=1.11.4
pyyaml>=6.0.1
joblib>=1.3.2
requests>=2.31.0
tzdata>=2024.1  # zoneinfo tz database (Etc/GMT-5 localization on minimal Linux)

# Models exercised by scripts/tests
scikit-learn>=1.3.2
xgboost>=2.0.3
lightgbm>=4.1.1
catboost>=1.2.2

# Solar geometry / clear-sky
pvlib>=0.10.3

# API + dashboard (imported by tests / lint)
fastapi>=0.109.0
httpx>=0.27.0
streamlit>=1.29.0
plotly>=5.18.0

# Tooling
pytest>=7.4.3
pytest-cov>=4.1.0
pytest-github-actions-annotate-failures>=0.2.0
pylint>=3.0.3
pyright>=1.1.390
