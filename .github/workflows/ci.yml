name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: requirements-ci.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-ci.txt

      - name: Lint (pylint)
        # Repo is at 10/10; the floor leaves a small margin for minor additions.
        run: pylint --fail-under=9.5 scripts api dashboard

      - name: Type check (pyright)
        run: pyright --project pyrightconfig.json

      - name: Tests (pytest)
        run: |
          set -o pipefail
          python -m pytest -q -rf --tb=short --no-header \
            --cov=scripts --cov=api --cov-report=term-missing 2>&1 | tee pytest-output.txt

      - name: Publish pytest output
        if: always()
        run: |
          {
            echo '## pytest'
            echo '```'
            tail -c 15000 pytest-output.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload pytest log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-output
          path: pytest-output.txt
